[tox]
envlist = py{311}

[testenv]
deps =
    -rrequirements-dev.txt
commands =
    pytest

[testenv:pre-commit]
skip_install = true
deps = pre-commit
commands = pre-commit run --all-files --show-diff-on-failure
